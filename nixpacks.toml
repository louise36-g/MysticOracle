[start]
cmd = '''printf ':{$PORT} {\nroot * ../app/{$NIXPACKS_SPA_OUTPUT_DIR}\nencode gzip\nheader /assets/* Cache-Control "public, max-age=31536000, immutable"\nheader /logos/* Cache-Control "public, max-age=2592000"\nheader /sw.js Cache-Control "no-cache, must-revalidate"\ntry_files {path} {path}/index.html /index.html\nfile_server\n}\n' > /assets/Caddyfile && caddy run --config /assets/Caddyfile --adapter caddyfile 2>&1'''
